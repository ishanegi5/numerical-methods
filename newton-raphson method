def f(x):
    return x**3 - x - 2

def df(x):
    return 3*x**2 - 1   # derivative

x = 1.5   # initial guess
tolerance = 0.0001

while True:
    x_new = x - f(x) / df(x)
    
    if abs(x_new - x) < tolerance:
        break
    
    x = x_new

print("Root is:", x_new)
